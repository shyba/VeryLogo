repos:
  - repo: local
    hooks:
      - id: black
        name: black
        entry: .venv/bin/black
        language: system
        types: [python]
      - id: unit-tests
        name: unit-tests
        entry: .venv/bin/python -m unittest discover -s tests
        language: system
        pass_filenames: false

